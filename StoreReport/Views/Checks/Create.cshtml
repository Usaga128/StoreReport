@model StoreReport.Models.Checks

@{
    ViewData["Title"] = "Creación de preguntas";
}

<h2>Crear Pregunta</h2>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Question" class="control-label">Pregunta</label>
                <input asp-for="Question" class="form-control" />
                <span asp-validation-for="Question" class="text-danger"></span>
            </div>
            <div class="form-group">
                <select class="custom-select" id="QuestionType" data-val="true" data-val-required="The Status field is required." name="QuestionType">
                    <option value="1">Si/No</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Agregar pregunta" class="btn btn-success" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Retornar a la lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
